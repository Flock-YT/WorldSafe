# F-01 至 F-05 修复验证记录

日期：2026-09-06。版本仍为 `1.1.0-beta.1`；本次未提交、推送代码或发布新版本。

## 自动化结果

| 检查 | 实际结果 |
| --- | --- |
| 修复前回归 | 新增测试分别复现 F-01 缺少方块变化事件、F-02 床另一端坐标漏判、F-03 有效快照被旧来源覆盖，共 3 项失败 |
| 修复后针对性测试 | 通过 |
| 最终 `mvn -B --no-transfer-progress clean verify` | JDK 17.0.19，Spigot 1.8.8 基线，71 项测试通过，0 失败／错误／跳过 |
| `scripts/verify-release-jar.sh` | 版本正确，所有打包类为 Java 8 或更早字节码，无 Bukkit API、禁用依赖或签名残留 |
| 发布脚本 | `bash -n` 通过；稳定版、alpha、beta、rc、snapshot、preview 参数测试通过；无真实发布调用 |
| 发布失败路径 | JAR 被修改、摘要侧文件被一起修改、摘要文件缺失、非法输入均在调用模拟 GitHub CLI 前失败 |
| 发布依赖 | YAML 结构测试确认同提交的可复用矩阵是 release 必需依赖，未用 `always()` 或 `continue-on-error` 绕过失败；产物检查先于上传 |
| 工作流静态校验 | actionlint 1.7.12 校验两个工作流通过；工具来自官方 Release，下载包已按发布方校验和核验 |
| Diff 检查 | `git diff --check` 通过 |

床测试包含旧／新数据接口、四个朝向和两端、错误邻居、反射异常、读取失败、快照与缓存双向冲突、空气快照、两秒 TTL 边界、整组一次性消费、覆盖记录及世界隔离。风弹测试组合两种风弹／无关实体、三种目标／普通方块、两条事件路径、配置／未配置世界，并确认不取消整个爆炸事件。

## IDEA 构建兼容修正

随后在 macOS 系统 Bash 3.2 下复现稳定版发布测试失败：`set -u` 会将空的 `channel_args` 数组视为未定义变量。发布脚本现改为组装始终非空的完整参数数组；测试显式覆盖 PATH 中的 `bash` 和 `/bin/bash`，失败时输出脚本日志。

使用 IDEA 2026.2.2 自带 Maven、用户提供的 Maven 参数、JDK 21.0.11 和仅包含系统工具的 PATH 执行 `package` 成功。随后用 JDK 17.0.19 执行 `clean verify`，71 项测试全部通过，并重新检查最终 JAR。本次仅修复构建脚本和测试，没有修改插件行为；下列 API 矩阵结果沿用前次验证，未重复运行。

## 编译矩阵：18 / 18 通过

所有 API 使用对应的 `-R0.1-SNAPSHOT` 依赖；命令与现有 CI 一致：

```sh
mvn -B --no-transfer-progress clean package -Dspigot.version=<API> -Dmaven.test.skip=true
```

| JDK | Spigot API 版本 | 结果 |
| --- | --- | --- |
| 17.0.19 | 1.8.8、1.9.4、1.10.2、1.11.2、1.12.2、1.13.2、1.14.4、1.15.2、1.16.5、1.17.1、1.18.2、1.19.4、1.20.3 | 13 / 13 |
| 21.0.11 | 1.20.6、1.21、1.21.11 | 3 / 3 |
| 25.0.3 | 26.1.2、26.2 | 2 / 2 |

本机编译日志位于 `run/verification/matrix/`（忽略目录，不入库）。矩阵完成后已重新用 JDK 17 和默认 Spigot 1.8.8 构建最终 JAR，未交付最后一个新版矩阵产物。

## 交付产物

- JAR：`target/WorldSafe-1.1.0-beta.1.jar`
- 校验文件：`target/WorldSafe-1.1.0-beta.1.jar.sha256`
- 本次构建 SHA-256：`f8fabae2620135ffddd1fa645406771960de9fefdb5948352927c70df9e690d7`

在 `target` 目录执行 `shasum -a 256 -c WorldSafe-1.1.0-beta.1.jar.sha256` 可复核。摘要仅对应本次构建；后续重新打包的时间戳可能改变摘要。

## 历史发布修正

已按用户授权将 [v1.1.0-beta.1](https://github.com/Flock-YT/WorldSafe/releases/tag/v1.1.0-beta.1) 设为 `prerelease=true`，并取消 Latest 分类。GitHub API 回读确认 Latest 为 `v1.0.6`、`prerelease=false`。

历史 Release ID `380265978`、标签、标题、说明、附件 ID `539123655` 均未改变。原附件摘要仍为 `c3603454305a38cd370c198407e39da58d537451d67e06f16a7080da81004393`。**该历史附件没有包含本次修复，不要用它替代本次本地 JAR。**

## 尚未验证的边界

- 未启动真实 Spigot／Paper 服务端或登录客户端；Java 8 字节码检查不代表已经在 Java 8 服务器上运行。
- 新版材质、实体及接口的单元测试使用替身；编译矩阵不验证真实事件顺序、伤害、击退或方块物理行为。
- 未推送代码，因此未在 GitHub 托管 runner 上运行新的发布依赖图，也未人为触发远端失败发布。依赖结构及脚本失败路径已本地自动验证。
- 这些差距不是需要用户重做的构建工作。用户只需完成 [精简实服清单](MANUAL_TEST_CN.md)；GitHub 托管工作流将在代码正常推送后验证，实际发布仍受版本触发边界约束。
- 恶魂反弹、自定义维度、其他完全取消爆炸功能及更多服务端组合为可选扩展验证，不声称已经修复报告中仅列为待确认的风险。
